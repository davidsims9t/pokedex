import { fireEvent } from '@testing-library/react';
import { test } from 'vitest';

test('fetches from pokeapi', async () => {

});

test('can search for pokemon', async () => {

});

test('has search history that can be revisited', async () => {

});